# Dependencies
pt_frontend/node_modules
pt_frontend/public/upload
pt_frontend/dist

# IDE
.vscode
.reasonix

# Local agent workspace (memory logs, session state)
.workbuddy/

# OS
.DS_Store
Thumbs.db

# docs
OPTIMIZATION-SUGGESTIONS.md

# Logs
*.log

# sessions — runtime data, never commit; the deny-all .htaccess MUST be tracked
pt_api/sessions/*
!pt_api/sessions/.htaccess

# Environment (contains real credentials — never commit)
pt_frontend/.env
pt_frontend/.env.local
pt_api/.env
pt_api/.env.local

# Install lock file
pt_api/.installed

# Build and release archives
release.zip
release-temp/